enum WheelsType {
    case racing
    case summer
    case winter
}

enum Illumination {
    case yes
    case no
}

enum Trailer {
    case yes
    case no
}

enum CustomMp3 {
    case have
    case haveNot
}
